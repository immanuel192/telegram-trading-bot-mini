---
trigger: model_decision
description: Mermaid diagram syntax and usage
---

# Tool Rules

## 8. Mermaid Rules

* Use parentless Mermaid syntax.
* Ensure Mermaid code is compatible with mermaid v8.8.0.
* Avoid parentheses inside node labels to prevent client crashes.
