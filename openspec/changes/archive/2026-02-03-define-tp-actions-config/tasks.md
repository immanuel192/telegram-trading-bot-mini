## 1. Data Model Updates

- [x] 1.1 Define `TpClosePercent` and `TpSlMoveLevel` types/unions in `libs/dal/src/models/account.model.ts`
- [x] 1.2 Define `TpAction` interface in `libs/dal/src/models/account.model.ts`
- [x] 1.3 Add TP monitoring configuration fields (`enableTpMonitoring`, `tp1Action`, etc.) to the `configs` object in `Account` interface
- [x] 1.4 Add comprehensive JSDoc documentation for the new fields as requested in the context

## 2. Verification

- [x] 2.1 Verify that the TypeScript project compiles without errors
- [x] 2.2 Cross-check the implementation against the requirement to support up to 4 TP tiers
